# 🔍 گزارش تحلیل توابع خواندنی (Read Functions) قراردادهای MyTitan

این گزارش شامل بررسی دقیق توابع خواندنی (View و Pure) در قراردادهای اصلی پروژه `MyTitan` است. در این سند مشخص شده است که هر قرارداد چه اطلاعاتی را در اختیار ما قرار می‌دهد و چگونه می‌توان از این داده‌ها در صفحات مختلف اپلیکیشن (فرانت‌اند) استفاده کرد.

---

## 🏛️ ۱. قرارداد Panel.sol (مرکز مدیریت اطلاعات ترکیبی)
این قرارداد به عنوان یک "نما (Facade)" عمل می‌کند و داده‌های چندین قرارداد دیگر را ترکیب کرده و به یکباره خروجی می‌دهد. برای فرانت‌اند، این قرارداد مهم‌ترین مرجع خواندن اطلاعات است زیرا از فراخوانی‌های متعدد جلوگیری می‌کند.

### 📜 توابع کلیدی و موارد استفاده:
- **`getUserFullInfo(address)`**:
  - **اطلاعات خروجی:** برگرداندن تمام اطلاعات پایه (شماره کاربری، وضعیت ثبت‌نام)، ساختار درختی (آدرس بالاسری، تعداد زیرمجموعه‌ها در پای چپ و راست) و آمارهای وضعیت تسویه (Cleanup).
  - **مکان استفاده:** **صفحه داشبورد (Dashboard)** و **کارت پروفایل (Profile Card)**. این تابع به تنهایی می‌تواند اکثر اطلاعات دموگرافیک کاربر را تامین کند.
  
- **`getUserBasicDetails(address)`** & **`getUserGroupTimestamps(address, groupIdx)`**:
  - **اطلاعات خروجی:** آیدی کاربر، زمان دقیق ثبت نام، و تاریخ انقضای پکیج‌های کاربر در هر گروه (Group 1 تا 7).
  - **مکان استفاده:** **صفحات My Info** و **بخش هشدار انقضا (Expiry Alerts)**.
  
- **`getUserIncomeCapInfo(address)`**:
  - **اطلاعات خروجی:** بالاترین گروهی که کاربر در آن پکیج فعال دارد.
  - **مکان استفاده:** **کارت پروفایل (بخش Rating - ستاره‌ها)** و **منطق دسترسی صفحات ویژه**.

- **`getNodeInfo(address)`**:
  - **اطلاعات خروجی:** اطلاعات فشرده در مورد یک نود خاص در درخت باینری (وضعیت خودش و فرزندان مستقیمش).
  - **مکان استفاده:** **صفحه Network / Tree View** برای رسم ساختار درختی باینری (Binary Tree).

---

## 🗃️ ۲. قرارداد Ledger.sol (دفتر کل اعتبارات و پوینت‌ها)
این قرارداد وضعیت ترازنامه‌ها، امتیازات توزیع‌شده و سهام RFT کاربران را ذخیره می‌کند.

### 📜 توابع کلیدی و موارد استفاده:
- **`userTotalRftBaseShares(address)`**:
  - **اطلاعات خروجی:** مجموع سهام پایه RFT (برای محاسبه سود بین‌المللی).
  - **مکان استفاده:** **صفحه Team Report** یا **بخش Dividends** برای نمایش میزان مشارکت کاربر در استخر جهانی.
  
- **`userPendingPoints(address, groupIdx)`** & **`userPointLedgers(address, groupIdx)`**:
  - **اطلاعات خروجی:** پوینت‌های خام (Raw Left & Right) و پوینت‌های پرداخت‌شده (Paid) در هر گروه.
  - **مکان استفاده:** **صفحه Team Report** برای نمایش امتیازات جمع شده در پای چپ و راست تیم.

- **`userPackageGroupExpiry(address, groupIdx)`**:
  - **مکان استفاده:** نمایش روزهای باقیمانده انقضای پکیج در **Store** و **Profile**.

- **`weeklyRevenueE1(weekId)`** & **`weeklyData(weekId)`**:
  - **مکان استفاده:** رسم چارت‌های درآمدی سیستم در **صفحه Analytics / Admin Dashboard**.

---

## 🧬 ۳. قرارداد Register.sol (مدیریت ثبت نام و درخت باینری)
اطلاعات پایه مربوط به لحظه ورود کاربران و موقعیت دقیق آن‌ها در گراف شبکه در اینجا قرار دارد.

### 📜 توابع کلیدی و موارد استفاده:
- **`isUserAddressRegistered(address)`** & **`getUserStatus(address)`**:
  - **اطلاعات خروجی:** آیا کیفی پول ثبت شده است؟ وضعیت فعلی کاربر چیست؟ (Free, Inactive, Active, Blocked).
  - **مکان استفاده:** **گارد مسیرها (Route Guards)** در فرانت‌اند. اگر وضعیت Free یا ثبت‌نام‌نشده باشد، کاربر را به **صفحه Login/Register** ریدایرکت می‌کنیم.
  
- **`getDirectChildren(address)`** & **`getParent(address)`**:
  - **مکان استفاده:** رسم گراف زیرمجموعه‌ها در **Network Tree**.

- **`getTotalRegisteredUsers()`**:
  - **مکان استفاده:** نمایش آمار "کل اعضای اکوسیستم" در **صفحه Home / Landing**.

---

## 🔍 ۴. قرارداد Lens.sol (رابط خواندنی تحلیلی - Analytics)
مشابه Panel، قرارداد Lens عملیات سنگین محاسباتی را روی سرور بلاکچین انجام می‌دهد تا کلاینت (فرانت‌اند) نیازی به پردازش‌های ریاضی پیچیده نداشته باشد.

### 📜 توابع کلیدی و موارد استفاده:
- **`getUserTotalPendingPoints(address)`**:
  - **اطلاعات خروجی:** مجموع کل پوینت‌های منتظر تراز در تمام گروه‌ها با هم.
  - **مکان استفاده:** به عنوان یک فاکتور بزرگ در **Dashboard** تحت عنوان "Pending Bonuses".
  
- **`getUserWeeklyStats(address, weekId)`**:
  - **مکان استفاده:** رسم نمودارهای هفتگی رشد در **Team Report**.

---

## ⚙️ ۵. قراردادهای Configs و Store و Bank (تنظیمات، فروشگاه و مالی)

### 📜 توابع کلیدی و موارد استفاده (Configs.sol):
- **`packagePriceUSD(groupIdx, pkgType)`** و **`rftBaseShares(groupIdx)`**:
  - **مکان استفاده:** **صفحه Store**. برای نمایش قیمت پکیج‌های Booster به صورت داینامیک و محاسبه سهام دریافتی.
- **`lifeFeeBPS()`** و **`claimFeeBPS()`**:
  - **مکان استفاده:** محاسبه کارمزدها قبل از فراخوانی تابع Withdraw در **صفحات برداشت (Withdrawal Modals)**.

### 📜 توابع کلیدی و موارد استفاده (Bank.sol):
- **`getErxPriceUsd()`**:
  - **مکان استفاده:** تبدیل دلار به توکن ERX به صورت زنده در **بالای همه صفحات (Header)** یا ماشین حساب سایت.

### 📜 توابع کلیدی و موارد استفاده (Store.sol):
- **`getProductIdsPaginated(offset, limit)`** و **`catalog(productId)`**:
  - **مکان استفاده:** بارگذاری محصولات واقعی فروشگاه (محصولات فیزیکی یا سرویس‌ها) با قابلیت Pagination در **صفحه Store (بخش عمومی)**.

---

## 💡 جمع‌بندی طلایی برای توسعه فرانت‌اند
به جای آنکه در یک صفحه چندین قرارداد را با هم فراخوانی (Call) کنید، معماری MyTitan طوری است که توابع تجمیعی خوبی دارد:
1. برای وضعیت کامل کاربر در **داشبورد** فقـــط از `Panel.getUserFullInfo` استفاده کنید.
2. برای آمار تیمی و امتیازها در **Team Report** از `Lens.getPointLedger` و `Lens.getUserTotalPendingPoints` استفاده کنید.
3. برای محاسبات مالی قبل از تراکنش از `Configs` و `Bank` قیمت‌های لحظه‌ای را دریافت کنید.
